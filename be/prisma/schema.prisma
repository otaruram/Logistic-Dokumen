generator client {
  provider  = "prisma-client-py"
  interface = "asyncio"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id              String   @id @default(cuid())
  email           String   @unique
  name            String?
  picture         String?
  
  tier            String   @default("FREE")
  creditBalance   Int      @default(3)      // User baru otomatis dapet 3
  lastCreditReset DateTime @default(now()) 
  createdAt       DateTime @default(now())  // Patokan reset data bulanan

  logs            Logs[]
  ratings         Rating[]
  transactions    CreditTransaction[]
}

model Logs {
  id           Int      @id @default(autoincrement())
  userId       String
  timestamp    DateTime @default(now())
  filename     String
  kategori     String?
  nomorDokumen String?
  receiver     String?
  imagePath    String?
  summary      String?
  fullText     String?

  user         User     @relation(fields: [userId], references: [email])
}

model Rating {
  id         Int      @id @default(autoincrement())
  userId     String
  userName   String
  userAvatar String?
  stars      Int
  emoji      String
  message    String
  createdAt  DateTime @default(now())

  user       User     @relation(fields: [userId], references: [email])
}

model CreditTransaction {
  id        Int      @id @default(autoincrement())
  userId    String
  amount    Int
  type      String   
  reason    String?
  timestamp DateTime @default(now())

  user      User     @relation(fields: [userId], references: [id])
}
